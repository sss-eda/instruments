#paths/instrument.yaml
get:
  description: Returns the instrument details.
  requestBody:
    description: 
    content:
    #required: false
  parameters:
  responses:
put:
  description: Updates the instrument.
  requestBody:
    description: New version of instrument
    content:
      application/json:
        schema:
          $ref: ./schemas/instrument.yaml
      #application/xml:
      #application/x-www-form-urlencoded:
        example:
          summary: Update instrument
          value:
            id: 1
            instrumentType: LEMI025
            commandTypes:
            - 
        #examples:
        encoding:
    #required: false
  parameters:
  responses:
patch:
  description: Partially updates the instrument.